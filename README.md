# To Run

`npm install`

start

- `npm run start`

`ctrl + c` to exit

follow start to update changes and reload

# to run with docker

`./docker-up.sh 5000`

change 5000 to whichever port you wish to run on
